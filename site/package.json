{
  "name": "graphql-mocks-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Landing site for graphql-mocks (Eleventy, deployed to GitHub Pages)",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
